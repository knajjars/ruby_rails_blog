<main class="container">
  <div class="p-4 p-md-5 mb-4 text-white rounded bg-dark">
    <div class="col-md-6 px-0">
      <h1 class="display-4 font-italic"><%= @topic.title %></h1>
      <%= link_to 'Back', topics_path %>
    </div>
  </div>

  <%= render @blogs %>
  <%== pagy_bootstrap_nav(@pagy) %>
</main>
